# Set this in the web host's build environment (for example, Vercel).
# The code also has this Render URL as a production fallback, but setting the
# variable explicitly makes a future API-domain change a configuration-only change.
NEXT_PUBLIC_API_BASE_URL=https://pg-platform-api.onrender.com/api/v1
